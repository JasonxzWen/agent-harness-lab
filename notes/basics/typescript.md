# TypeScript 学习笔记

优先围绕 agent harness 学 TypeScript。

## 当前要掌握

- `type` / `interface`
- union type
- discriminated union
- `async` / `await`
- `Promise<T>`
- `Record<string, unknown>`
- ESM `import` / `export`

## 学习问题

- 如何用类型描述 message？
- 如何用类型描述 tool？
- 如何避免工具输入里到处都是 `any`？
