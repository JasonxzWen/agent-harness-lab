# Ralph Progress Log
Started: 2026-05-08T00:00:00+08:00
Project: KnowledgeGraphCompletion
Branch: ralph/knowledge-graph-completion

## Codebase Patterns

- Knowledge graph work lives in `apps/knowledge-graph` and uses Bun, Vite, React, and TypeScript.
- Before editing UI stories, read `AGENTS.md`, `DESIGN.md`, and `apps/knowledge-graph/AGENTS.md`.
- Keep user-facing copy Chinese-first, short, and concrete. Avoid the forbidden filler terms listed in tests.
- Do not submit `.external/`, `node_modules`, `dist`, `labs/ts-agent/workspace`, or copied CCB / skill-hub / third-party body text.
- Use existing typed graph data and React/CSS first. Do not add a new icon library.

---

## 2026-05-08T16:35:00+08:00 - Ralph runner check
- What was checked
  - Ran `Set-Location D:\agent-harness-lab; powershell -ExecutionPolicy Bypass -File scripts\ralph\ralph.ps1 -MaxIterations 12`.
  - `codex` CLI was available at `C:\Users\Admin\AppData\Local\OpenAI\Codex\bin\codex.exe`.
  - The first Codex CLI iteration selected `US-001`, then stopped because the CLI session did not expose the Browser skill's required `node_repl js` / `mcp__node_repl__js` tool.
- Files changed
  - `scripts/ralph/ralph.ps1`
  - `.gitignore`
  - `scripts/ralph/progress.txt`
- Checks run
  - `Get-Content -LiteralPath scripts\ralph\prd.json -Raw -Encoding UTF8 | ConvertFrom-Json`
- Learnings for future iterations:
  - Windows PowerShell needs explicit `-Encoding UTF8` when reading Chinese `prd.json`.
  - This local `codex exec` uses `--sandbox`, not `--ask-for-approval`.
  - Ralph must check `--output-last-message` output for `<promise>COMPLETE</promise>` so it does not match the prompt text.
  - UI stories cannot pass in this CLI environment unless the in-app browser tool is exposed.
---

## 2026-05-08T17:00:00+08:00 - US-001
- What was implemented
  - Reworked the graph canvas into a draggable, zoomable graph viewport.
  - Rendered all graph nodes by layer, rendered SVG edges, and highlighted the Beginner Path.
  - Kept the detail drawer click path and visible `暂未实现` copy for search, theme filtering, path switching, and progress persistence.
- Files changed
  - `apps/knowledge-graph/src/components/graph/KnowledgeGraphCanvas.tsx`
  - `apps/knowledge-graph/src/styles/app-shell.css`
- Checks run
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run typecheck`
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun test`
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run build`
- Browser screenshot result
  - Blocked. The current Codex in-app browser can open public pages, but local targets such as `http://127.0.0.1:5176/`, `http://localhost:5175/`, host IP URLs, and `file://` build output are rejected by the browser client or policy.
  - Because the required in-app browser screenshot could not be completed, `US-001` remains `passes=false`.
- Hover / focus / active / click feedback
  - Graph nodes use border, underline, opacity, and token background changes.
  - Zoom controls use border and underline feedback.
  - No `box-shadow`, gradient, scale hover, or icon library was added.
- Global thinking check
  - The graph adds more visible nodes, but contains them inside a bounded viewport to avoid pushing the drawer and toolbar.
  - The first action remains clear: drag the canvas, zoom the viewport, then click a node.
  - Unfinished controls still show `暂未实现`.
- why / what / how
  - Existing drawer still shows `why / what / how`; this story did not change detail content.
- Learnings for future iterations:
  - In this desktop session, local in-app browser verification is blocked even though public navigation works.
  - Do not mark UI stories passing until the local in-app browser route is fixed or the user explicitly approves a different visual verification path.
---

## 2026-05-08T17:20:00+08:00 - US-001 completion
- What was implemented
  - Marked `US-001` complete after the user approved local Playwright screenshots as the visual fallback.
  - Repaired `scripts/ralph/prd.json` as UTF-8 JSON so future Ralph passes keep Chinese story titles and acceptance criteria readable.
  - Tightened the graph panel side title to avoid text wrapping into the canvas.
- Files changed
  - `.gitignore`
  - `apps/knowledge-graph/src/components/graph/KnowledgeGraphCanvas.tsx`
  - `apps/knowledge-graph/src/styles/app-shell.css`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
  - `output/playwright/us001-desktop.png`
  - `output/playwright/us001-detail.png`
- Checks run
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run typecheck` passed.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun test` passed, 10 tests.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run build` passed.
- Browser screenshot result
  - Local in-app browser remained blocked for local URLs.
  - Playwright fallback screenshots passed: `output/playwright/us001-desktop.png` and `output/playwright/us001-detail.png`.
- Hover / focus / active / click feedback
  - Nodes expose hover and focus by border, underline, and token background.
  - Active press uses the same token palette without scale or shadow.
  - Click opens the detail drawer and changes the selected node progress label to `学习中`.
  - Zoom controls use visible border and underline feedback.
- Global thinking check
  - The graph is dense, but it is contained in a bounded pan/zoom viewport and no longer pushes the drawer.
  - The first action is visible: drag the canvas, zoom, then click a node.
  - Search, theme filter, path switch, and progress persistence still show `暂未实现`.
- why / what / how
  - Detail drawer keeps the existing `why / what / how` visual cards.
  - Dedicated per-node visual upgrades remain scoped to `US-004`.
---

## 2026-05-08T17:30:00+08:00 - US-002 completion
- What was implemented
  - Added a reserved summary card between graph controls and the viewport.
  - Hover and keyboard focus on a node show title, summary, prerequisites, and recommended next nodes.
  - Click keeps the selected node as the summary source, which gives touch users an equivalent entry without hover.
- Files changed
  - `apps/knowledge-graph/src/components/graph/KnowledgeGraphCanvas.tsx`
  - `apps/knowledge-graph/src/styles/app-shell.css`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
  - `output/playwright/us002-hover.png`
  - `output/playwright/us002-click.png`
- Checks run
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run typecheck` passed.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun test` passed, 10 tests.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run build` passed.
- Browser screenshot result
  - Local in-app browser remained blocked for local URLs.
  - Playwright fallback screenshots passed: `output/playwright/us002-hover.png` and `output/playwright/us002-click.png`.
- Hover / focus / active / click feedback
  - Hover and focus update the summary card without covering the node or drawer.
  - Active and selected node feedback still uses token background and border only.
  - Click opens detail and keeps the same node summary visible for touch use.
- Global thinking check
  - The summary card adds one area, but it is reserved space, so the graph does not jump when hover changes.
  - The user path is clear: scan summary first, then click for detail.
  - Search, theme filter, path switch, and progress persistence still show `暂未实现`.
- why / what / how
  - The summary card is intentionally short; full `why / what / how` remains in the detail drawer.
  - Dedicated per-node visual upgrades remain scoped to `US-004`.
---

## 2026-05-08T17:34:00+08:00 - US-003 completion
- What was implemented
  - Added drawer open focus: the close button receives focus when a node detail opens.
  - Added close focus return: `Esc` or the close button closes the drawer and returns focus to the original node.
  - Marked the drawer as a labelled `dialog` region while keeping it non-modal.
- Files changed
  - `apps/knowledge-graph/src/components/drawer/DetailDrawer.tsx`
  - `apps/knowledge-graph/src/components/graph/KnowledgeGraphCanvas.tsx`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
  - `output/playwright/us003-open.png`
  - `output/playwright/us003-esc-return.png`
- Checks run
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run typecheck` passed.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun test` passed, 10 tests.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run build` passed.
- Browser screenshot result
  - Local in-app browser remained blocked for local URLs.
  - Playwright fallback screenshots passed: `output/playwright/us003-open.png` and `output/playwright/us003-esc-return.png`.
- Hover / focus / active / click feedback
  - Click or native button Enter opens detail.
  - Open state shows focus on the close button.
  - `Esc` closes detail and returns focus to the node, keeping the summary visible.
  - Close button hover, focus, active states continue to use border, underline, and token background.
- Global thinking check
  - No new permanent panel was added; this story only improves the existing drawer path.
  - The keyboard path is now predictable: node, drawer close, back to node.
  - Search, theme filter, path switch, and progress persistence still show `暂未实现`.
- why / what / how
  - The drawer still starts with `why / what / how`; this story changes access and focus behavior only.
  - Dedicated per-node visual upgrades remain scoped to `US-004`.
---

## 2026-05-08T17:40:00+08:00 - US-004 completion
- What was implemented
  - Added a `why / what / how` visual path at the top of the detail drawer.
  - The path is generated from each node's detail copy and first source path, so every node gets the same visual structure with node-specific content.
  - Tuned the visual path from cramped columns to a vertical three-step layout inside the narrow drawer.
- Files changed
  - `apps/knowledge-graph/src/components/drawer/DetailDrawer.tsx`
  - `apps/knowledge-graph/src/styles/app-shell.css`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
  - `output/playwright/us004-visual.png`
- Checks run
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run typecheck` passed.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun test` passed, 10 tests.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run build` passed.
- Browser screenshot result
  - Local in-app browser remained blocked for local URLs.
  - Playwright fallback screenshot passed: `output/playwright/us004-visual.png`.
- Hover / focus / active / click feedback
  - The story adds a passive visual element; existing node hover, focus, active, and click feedback remains unchanged.
  - Drawer close feedback remains visible after the US-003 focus work.
- Global thinking check
  - The drawer now has one more visual block, but it is directly above the existing text cards and uses the same border language.
  - The three-step layout avoids narrow-column crowding and keeps the first visible detail focused.
  - Search, theme filter, path switch, and progress persistence still show `暂未实现`.
- why / what / how
  - `why`, `what`, and `how` are now visible both as cards and as a compact path.
  - The visual path uses only local node copy and source-path metadata.
---

## 2026-05-08T17:46:00+08:00 - US-005 completion
- What was implemented
  - Added a search input to the graph controls.
  - Search matches display title, summary, tags, lab source paths, and CCB mapping paths.
  - Filtered graph nodes, visible edges, header counts, and theme counts update immediately.
  - Empty results show a Chinese message and a clear-search action.
- Files changed
  - `apps/knowledge-graph/src/components/graph/KnowledgeGraphCanvas.tsx`
  - `apps/knowledge-graph/src/styles/app-shell.css`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
  - `output/playwright/us005-search.png`
  - `output/playwright/us005-empty.png`
- Checks run
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run typecheck` passed.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun test` passed, 10 tests.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run build` passed.
- Browser screenshot result
  - Local in-app browser remained blocked for local URLs.
  - Playwright fallback screenshots passed: `output/playwright/us005-search.png` and `output/playwright/us005-empty.png`.
- Hover / focus / active / click feedback
  - Search input hover, focus, and active states switch to the token background and keep visible text contrast.
  - Clear buttons use the same border, underline, and token background feedback as graph controls.
  - Node click and drawer behavior remain unchanged.
- Global thinking check
  - Search adds one control above zoom, but it sits inside the existing graph controls and does not cover nodes.
  - Empty results are explicit and provide a single recovery action.
  - Theme filter, path switch, and progress persistence still show `暂未实现`.
- why / what / how
  - Search does not add new knowledge content.
  - Filtered nodes still open the drawer with the `why / what / how` visual path from `US-004`.
---

## 2026-05-08T17:50:00+08:00 - US-006 completion
- What was implemented
  - Added theme filter controls for all current theme labels.
  - Theme selection filters nodes and edges after search matching, so search and theme work together.
  - Header counts, theme counts, and empty states update with the active theme.
  - Added clear-theme and all-theme actions.
- Files changed
  - `apps/knowledge-graph/src/components/graph/KnowledgeGraphCanvas.tsx`
  - `apps/knowledge-graph/src/styles/app-shell.css`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
  - `output/playwright/us006-theme.png`
  - `output/playwright/us006-clear.png`
- Checks run
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run typecheck` passed.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun test` passed, 10 tests.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run build` passed.
- Browser screenshot result
  - Local in-app browser remained blocked for local URLs.
  - Playwright fallback screenshots passed: `output/playwright/us006-theme.png` and `output/playwright/us006-clear.png`.
- Hover / focus / active / click feedback
  - Theme buttons use the same border, underline, and token background language as search and zoom controls.
  - The selected theme has a persistent token background.
  - Click on `全部` or `清除主题` restores the full graph.
- Global thinking check
  - The filter adds several small buttons, but they are grouped under one label and stay above the graph viewport.
  - The selected state is visible; users can recover through `全部` or `清除主题`.
  - Path switch and progress persistence still show `暂未实现`.
- why / what / how
  - Theme filtering does not add new knowledge content.
  - Filtered nodes still open the drawer with the `why / what / how` visual path from `US-004`.
---

## 2026-05-08T17:56:00+08:00 - US-007 completion
- What was implemented
  - Added four learning path buttons: Beginner, Context, Safety, and Advanced.
  - Current path summary and visible step count are shown above the graph.
  - Current path nodes and edges keep full visual weight; other nodes remain accessible with lower weight.
  - Removed the `机制地图` side title after user feedback, freeing the left column for the graph.
- Files changed
  - `apps/knowledge-graph/src/components/graph/KnowledgeGraphCanvas.tsx`
  - `apps/knowledge-graph/src/styles/app-shell.css`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
  - `output/playwright/us007-path.png`
  - `output/playwright/us007-advanced.png`
- Checks run
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run typecheck` passed.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun test` passed, 10 tests.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run build` passed.
- Browser screenshot result
  - Local in-app browser remained blocked for local URLs.
  - Playwright fallback screenshots passed: `output/playwright/us007-path.png` and `output/playwright/us007-advanced.png`.
- Hover / focus / active / click feedback
  - Path buttons use the same border, underline, and token background language as search and theme filters.
  - The selected path has a persistent token background.
  - Click immediately updates route summary, trace route, path node emphasis, and active edges.
- Global thinking check
  - Removing the side title fixed the uneven layout and gave the graph more horizontal space.
  - The controls are more numerous now, but they are grouped by search, theme, path, and viewport controls.
  - Progress persistence still shows `暂未实现`.
- why / what / how
  - Path mode does not add new knowledge content.
  - Nodes opened from any path still show the `why / what / how` visual path from `US-004`.
---

## 2026-05-08T18:02:00+08:00 - US-008 completion
- What was implemented
  - Added per-node progress states: `not-started`, `learning`, `implemented`, and `reviewed`.
  - Progress is persisted in `localStorage` and restored after reload.
  - Clicking a new node moves it from `not-started` to `learning`.
  - Added drawer progress buttons and a graph-level `清除本地进度` action.
- Files changed
  - `apps/knowledge-graph/src/components/graph/KnowledgeGraphCanvas.tsx`
  - `apps/knowledge-graph/src/components/drawer/DetailDrawer.tsx`
  - `apps/knowledge-graph/src/styles/app-shell.css`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
  - `output/playwright/us008-reviewed.png`
  - `output/playwright/us008-restored.png`
  - `output/playwright/us008-cleared.png`
- Checks run
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run typecheck` passed.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun test` passed, 10 tests.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run build` passed.
- Browser screenshot result
  - Local in-app browser remained blocked for local URLs.
  - Playwright fallback screenshots passed: `output/playwright/us008-reviewed.png`, `output/playwright/us008-restored.png`, and `output/playwright/us008-cleared.png`.
- Hover / focus / active / click feedback
  - Progress buttons use the same border, underline, and token background language as other controls.
  - Selected progress has a persistent token background.
  - Clear progress visibly resets the summary count and node labels.
- Global thinking check
  - The progress area adds useful state without adding a new page section.
  - It sits inside existing graph controls, so the interaction path stays local to the graph.
  - Reference panel, mobile polish, and final audit remain incomplete.
- why / what / how
  - Progress does not add new knowledge content.
  - Nodes still open the drawer with the `why / what / how` visual path from `US-004`.
---

## 2026-05-08T18:12:00+08:00 - US-009 completion
- What was implemented
  - Replaced the drawer source-path list with a grouped reference panel.
  - The panel groups local docs, lab source, CCB mapping paths, and external links.
  - Each reference shows title, type, path, optional note, and a copy-path action.
  - Clipboard failure is ignored safely while keeping visible click feedback.
- Files changed
  - `apps/knowledge-graph/src/components/drawer/DetailDrawer.tsx`
  - `apps/knowledge-graph/src/styles/app-shell.css`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
  - `output/playwright/us009-references.png`
- Checks run
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run typecheck` passed.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun test` passed, 10 tests.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run build` passed.
- Browser screenshot result
  - Local in-app browser remained blocked for local URLs.
  - Playwright fallback screenshot passed: `output/playwright/us009-references.png`.
- Hover / focus / active / click feedback
  - Reference copy buttons use border, underline, focus outline, and active background feedback.
  - Click changes the current button text from `复制路径` to `已复制`.
  - Empty reference groups are filtered out before render.
- Global thinking check
  - The reference panel sits inside the existing drawer; it does not add a new top-level section.
  - The panel adds more detail only after why / what / how, so the first read path stays clear.
  - Search, filter, path mode, and progress controls remain unchanged.
- why / what / how
  - Reference panel does not add new knowledge content.
  - Nodes still open the drawer with the `why / what / how` visual path from `US-004`.
---

## 2026-05-08T18:18:00+08:00 - US-010 completion
- What was implemented
  - Added keyboard focus reveal for graph nodes, so off-screen focused nodes move into the graph viewport.
  - Tightened mobile graph layout with a fixed draggable viewport and smaller inner padding.
  - Restored theme, path, and progress controls to their own layout groups.
  - Updated feature card copy so search, filter, path, and progress are no longer marked pending.
  - Added reduced-motion guard for scroll behavior.
- Files changed
  - `apps/knowledge-graph/src/components/graph/KnowledgeGraphCanvas.tsx`
  - `apps/knowledge-graph/src/components/layout/AppShell.tsx`
  - `apps/knowledge-graph/src/styles/app-shell.css`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
  - `output/playwright/us010-mobile.png`
  - `output/playwright/us010-keyboard-focus.png`
  - `output/playwright/us010-desktop.png`
- Checks run
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run typecheck` passed.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun test` passed, 10 tests.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run build` passed.
- Browser screenshot result
  - Local in-app browser remained blocked for local URLs.
  - Playwright fallback screenshots passed: `output/playwright/us010-mobile.png`, `output/playwright/us010-keyboard-focus.png`, and `output/playwright/us010-desktop.png`.
- Hover / focus / active / click feedback
  - Toolbar links now have visible focus outlines.
  - Graph controls keep the same border, underline, active fill, and focus outline feedback.
  - Keyboard node focus updates the summary card and moves the viewport to the focused node.
- Global thinking check
  - The graph no longer stretches the mobile page with the full internal graph height.
  - The existing graph controls stay grouped; no new top-level UI area was added.
  - Pending copy now names only content boundary checks and final visual review.
- why / what / how
  - Accessibility polish does not add new knowledge content.
  - Nodes still open the drawer with the `why / what / how` visual path from `US-004`.
---
