# Ralph Progress Log
Started: 2026-05-08T20:06:24.8244825+08:00
---

## Codebase Patterns

- UI story screenshots are saved under `output/playwright/`; first-viewport stories should verify 1365x768 desktop before marking passing.
- If `npx @playwright/cli` cannot reach npm, local Playwright can run from the cached runtime package with the installed Chrome executable.
- Dense secondary UI should use the marker-plus-slideout pattern: keep a short marker visible, reveal details on hover/focus, and allow click to pin one card.

---

## 2026-05-08T20:22:34+08:00 - US-001
- What was implemented
  - Reworked the first screen into one guided learning entry for the Beginner Path.
  - Hero copy now names the start point directly and avoids repeating graph-control explanations.
  - Moved the old capability cards into a compact status row below the graph.
  - Removed the top toolbar CTA so the primary action is the learning-entry button.
- Files changed
  - `apps/knowledge-graph/src/components/layout/AppShell.tsx`
  - `apps/knowledge-graph/src/components/layout/GraphToolbar.tsx`
  - `apps/knowledge-graph/src/styles/app-shell.css`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
  - `output/playwright/us001-entry-desktop.png`
  - `output/playwright/us001-entry-hover-focus.png`
  - `output/playwright/us001-entry-active.png`
  - `output/playwright/us001-entry-click-map.png`
- Checks run
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run typecheck` passed.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun test` passed, 16 tests.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run build` passed.
- Commit status
  - Commit was attempted, but `git add` failed with `Unable to create '.git/index.lock': Permission denied`.
  - `icacls .git` shows an explicit write deny ACL in this sandbox, so the story changes could not be committed from this run.
- Browser screenshot result
  - Local Playwright screenshots passed for desktop first screen, hover/focus, active press, and click-to-map.
  - Desktop screenshots assert the continue button is visible in the first viewport.
- Hover / focus / active / click feedback
  - Continue button hover switches to token dark background and underline.
  - Keyboard focus adds a visible outline without box-shadow, gradient, or scale.
  - Active press keeps the same restrained token treatment.
  - Click jumps to `#map`, showing the graph as the next learning step.
- Global thinking check
  - The first screen has fewer competing actions after removing the toolbar CTA.
  - The old capability cards no longer compete with the hero; they are now a compact status row below the graph.
  - Desktop entry layout keeps the recommended path, first node, and continue button visible before the user chooses the next step.
- why / what / how
  - The learning entry includes a compact `why / what / how` sequence below the primary action.
  - The existing node detail drawer still provides the deeper per-node why / what / how visual.
- Learnings for future iterations:
  - Put the main CTA directly after the first required decision; secondary progress and explanation can follow.
  - First-viewport screenshots should assert button visibility, not only render the page.
---

## 2026-05-09T10:37:54+08:00 - US-002
- What was implemented
  - Kept search, current path, and next step visible in the default graph controls.
  - Moved theme filtering, layout choice, progress import/export, and zoom into a stacked control-card area.
  - Each advanced control keeps a visible marker; hover/focus slides out details and click pins one card open.
  - Added a visible next-step button that opens the first or next path node.
- Files changed
  - `apps/knowledge-graph/src/components/graph/KnowledgeGraphCanvas.tsx`
  - `apps/knowledge-graph/src/styles/app-shell.css`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
  - `output/playwright/us002-controls-collapsed.png`
  - `output/playwright/us002-controls-expanded.png`
- Checks run
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run typecheck` passed.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun test` passed, 16 tests.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run build` passed.
- Browser screenshot result
  - Local Playwright screenshots passed for collapsed desktop controls and hover/focus/click expanded controls.
  - The screenshot script asserted zero pinned advanced cards by default, one pinned card after click, and no desktop horizontal overflow.
- Hover / focus / active / click feedback
  - Control-card markers invert background and underline on hover, focus, and active.
  - Details slide in from the marker area with opacity/translate animation; no box-shadow, gradient, or scale.
  - Clicking a marker pins that card; clicking the next-step button opens the path node.
- Global thinking check
  - Default visible controls are reduced to search, current path, next step, and four summary rows.
  - The graph starts closer to the top because low-frequency controls no longer occupy the default panel.
  - The first action in the graph block is clear: search, choose a path, or open the next step.
- why / what / how
  - This story changes graph controls only.
  - The learning entry and node detail drawer still keep visible why / what / how sequences.
- Learnings for future iterations:
  - Use the marker-plus-slideout card pattern for secondary controls and dense supporting information.
  - Avoid Chinese literal assertions in inline PowerShell-to-Node Playwright scripts; use CSS selectors or Unicode escapes.
---

## 2026-05-09T10:56:42+08:00 - US-003
- What was implemented
  - Added a central bottom-up route guide inside the graph panel for the active learning path.
  - Replaced the old compact trace row with a Mermaid-like route index that shows current path, every step, started count, and next step.
  - Clicking a route step opens that node in the detail drawer, so the learner can jump from the route to why / what / how.
- Files changed
  - `apps/knowledge-graph/src/components/graph/KnowledgeGraphCanvas.tsx`
  - `apps/knowledge-graph/src/styles/app-shell.css`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
  - `output/playwright/us003-route-map-desktop.png`
  - `output/playwright/us003-route-map-click-agent-loop.png`
- Checks run
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run typecheck` passed.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun test` passed, 16 tests.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run build` passed.
- Browser screenshot result
  - Local Playwright screenshots passed for desktop route guide and clicking Agent Loop from the route.
  - The screenshot script asserted 9 route steps and no desktop horizontal overflow.
- Hover / focus / active / click feedback
  - Route step buttons invert background and underline on hover, focus, and active.
  - Keyboard focus uses a visible outline.
  - Click opens the selected node detail and marks the path step as current.
- Global thinking check
  - The path guide now gives one visible next action instead of making users infer it from the node layout.
  - The old trace row was removed, so the new guide adds direction without duplicating another summary strip.
  - Desktop route steps remain inside the graph panel without duplicating another summary strip.
- why / what / how
  - The route guide explains why it exists in one sentence: start from the bottom first step.
  - The guide itself is the what: a clickable path index.
  - The how is visible after click because the detail drawer opens the selected node's why / what / how and source path.
- Learnings for future iterations:
  - The main learning manual should be a clickable map, not a table.
  - Keep route labels short; detailed explanation belongs in the drawer opened by the route step.
---

## 2026-05-09T11:11:12+08:00 - US-004
- What was implemented
  - Added a folded feature preview stack after the route guide.
  - The stack covers search, path, progress, and code preview with lightweight React/CSS visuals.
  - Default state shows only marker rows; hover, focus, or click slides a card open.
  - Click pins one preview card so touch and keyboard users can inspect the effect.
- Files changed
  - `apps/knowledge-graph/src/components/graph/KnowledgeGraphCanvas.tsx`
  - `apps/knowledge-graph/src/styles/app-shell.css`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
  - `output/playwright/us004-feature-preview-collapsed.png`
- Checks run
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run typecheck` passed.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun test` passed, 16 tests.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run build` passed.
- Browser screenshot result
  - Local Playwright screenshots passed for folded desktop previews.
  - The screenshot script asserted default folded state and no desktop horizontal overflow.
- Hover / focus / active / click feedback
  - Preview markers open on hover and focus through the same slide-out CSS state.
  - Marker active state underlines the label without box-shadow, gradient, or scale.
  - Click toggles a pinned preview card.
- Global thinking check
  - The new preview area adds four feature examples without adding four full cards to the default view.
  - The route guide remains the main manual; previews sit below it as short supporting proof.
  - Desktop keeps markers folded by default and opens one card inside the panel.
- why / what / how
  - Why: users can see what a feature changes before trying it.
  - What: each marker names one feature and exposes a compact visual example.
  - How: React state pins the active preview, while CSS hover/focus reveals the same card for quick inspection.
- Learnings for future iterations:
  - Use folded marker rows for supporting demos so the route map stays central.
  - Add visual examples beside feature names before adding more explanatory text.
---

## 2026-05-09T11:24:11+08:00 - US-005
- What was implemented
  - Added typed code preview metadata to allowed `labs/ts-agent/src` source references.
  - Reference items with code previews now open a short snippet on hover or keyboard focus.
  - CCB mappings, external links, and local docs show boundary notes only; they do not expose body text.
  - Updated active specs to state that mobile design, code, build, and tests are not required.
  - Removed mobile-specific CSS breakpoints and tracked mobile screenshot artifacts.
- Files changed
  - `apps/knowledge-graph/src/types/graph.ts`
  - `apps/knowledge-graph/src/data/references.ts`
  - `apps/knowledge-graph/src/components/drawer/DetailDrawer.tsx`
  - `apps/knowledge-graph/src/styles/app-shell.css`
  - `apps/knowledge-graph/tests/knowledgeGraph.test.js`
  - `apps/knowledge-graph/scripts/visual-regression.mjs`
  - Active docs and Ralph specs with mobile requirements removed.
  - `output/playwright/us005-source-preview-focus-desktop.png`
  - `output/playwright/us005-source-preview-boundary-desktop.png`
- Checks run
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run typecheck` passed.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun test` passed, 16 tests.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run build` passed.
- Browser screenshot result
  - Local Playwright screenshots passed for desktop keyboard focus opening a lab code preview.
  - Local Playwright screenshots passed for desktop CCB boundary copy.
- Hover / focus / active / click feedback
  - Lab references invert background and slide slightly on hover or focus.
  - Keyboard focus opens the same preview as hover.
  - Copy buttons keep visible hover, focus, active, and copied states.
- Global thinking check
  - Code appears only inside the reference row that needs it, so the drawer does not gain a permanent code wall.
  - Boundary copy is short and next to the path, so users know why CCB and external sources do not expand.
  - The app now follows desktop-only verification; mobile-specific code and screenshots were removed.
- why / what / how
  - Why: users can connect a mechanism to local code without leaving the page.
  - What: each lab reference may carry one short typed snippet.
  - How: `SourceReference.codePreview` drives the drawer preview; tests enforce that only lab sources can use it.
- Learnings for future iterations:
  - Keep snippets short enough to scan inside a reference row.
  - Boundary notes should be explicit whenever a source must not expand.
---

## 2026-05-09T11:31:27+08:00 - US-006
- What was implemented
  - Detail drawer now keeps why / what / how and the main code entry visible first.
  - Quiz, source references, Bun commands, misconception, and comparison moved into folded marker cards.
  - Each marker opens on hover or focus and can be pinned open by click.
- Files changed
  - `apps/knowledge-graph/src/components/drawer/DetailDrawer.tsx`
  - `apps/knowledge-graph/src/styles/app-shell.css`
  - `output/playwright/us006-drawer-default-desktop.png`
  - `output/playwright/us006-drawer-references-open-desktop.png`
- Checks run
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run typecheck` passed.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun test` passed, 16 tests.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run build` passed.
- Browser screenshot result
  - Desktop default drawer screenshot passed with all supporting groups closed.
  - Desktop source group screenshot passed after clicking the SOURCE marker.
  - The screenshot script asserted no horizontal overflow and Esc focus return to the selected graph node.
- Hover / focus / active / click feedback
  - Fold markers switch background with their card on hover and focus.
  - Focus uses a visible outline and underline; active/click pins the selected group with `aria-expanded`.
  - Esc still closes the drawer and returns focus to the graph node.
- Global thinking check
  - The drawer is shorter by default because secondary panels are folded behind five labels.
  - The first visible actions are still why / what / how and code entry, so the next step is clear.
  - No mobile design, code, build, or test path is required under the active spec.
- why / what / how
  - Why: users should not scan every reference, command, and quiz before reading the core explanation.
  - What: secondary detail content is grouped as a marker stack with short labels.
  - How: React tracks one pinned group while CSS hover/focus reveals the same body for quick inspection.
- Learnings for future iterations:
  - Keep dense supporting content folded unless it is the next likely action.
  - Use marker labels as the table of contents instead of adding a text-heavy explanation block.
---

## 2026-05-09T11:52:26+08:00 - US-007
- What was implemented
  - Added one shared interaction state across route steps, graph nodes, edges, summary, and detail area.
  - Route hover/focus now marks the same node in the route map, graph canvas, and summary card.
  - Clicked nodes keep the detail drawer selected; focusing another route step parks the drawer while the summary previews the new node.
- Files changed
  - `apps/knowledge-graph/src/components/graph/KnowledgeGraphCanvas.tsx`
  - `apps/knowledge-graph/src/components/drawer/DetailDrawer.tsx`
  - `apps/knowledge-graph/src/styles/app-shell.css`
  - `output/playwright/us007-interaction-hover-desktop.png`
  - `output/playwright/us007-interaction-focus-desktop.png`
- Checks run
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run typecheck` passed.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun test` passed, 16 tests.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run build` passed.
- Browser screenshot result
  - Desktop hover screenshot passed for route-to-graph-to-summary feedback.
  - Desktop click/focus screenshot passed for selected drawer plus another focused preview.
- Hover / focus / active / click feedback
  - Hover and focus use border, underline, background switch, and CSS transitions.
  - Click opens the detail drawer and keeps the selected node visible.
  - The active detail drawer shows selected state; previewing another node does not replace the selected detail content.
  - Existing `prefers-reduced-motion` rule still reduces transition duration.
- Global thinking check
  - No new panel was added; existing route, graph, summary, and drawer now share state.
  - The first step and next step remain visible; interaction feedback adds clarity without more text.
  - Desktop-only verification remains active; no mobile path is required.
- why / what / how
  - Why: users need to feel which part changed when they hover, focus, or click.
  - What: one interaction state now marks the active node across route, canvas, summary, and detail.
  - How: React derives `interactionState` and `data-preview` attributes; CSS handles border, underline, background, and reduced-motion behavior.
- Learnings for future iterations:
  - Prefer shared state attributes over extra explanatory copy.
  - Keep graph hover screenshots stable by targeting route steps when canvas transform affects pointer hit testing.
---

## 2026-05-09T11:59:52+08:00 - US-008
- What was implemented
  - Updated README, development plan, and milestones for the completed clarity refactor.
  - Reworked `visual:regression` to use blocking Playwright CLI commands instead of async run-code snippets.
  - Desktop visual regression now covers first screen, detail drawer, and source hover preview.
- Files changed
  - `apps/knowledge-graph/README.md`
  - `apps/knowledge-graph/docs/development-plan.md`
  - `apps/knowledge-graph/docs/milestones.md`
  - `apps/knowledge-graph/scripts/visual-regression.mjs`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- Checks run
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run generate:references` passed, 25 reference index items.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run typecheck` passed.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun test` passed, 16 tests.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run build` passed.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run visual:regression` passed.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run build:pages` passed.
- Browser screenshot result
  - `output/playwright/regression/desktop.png` passed for the desktop first screen.
  - `output/playwright/regression/detail-drawer.png` passed for the selected detail drawer.
  - `output/playwright/regression/source-preview.png` passed for the source hover preview.
- Hover / focus / active / click feedback
  - Final regression covers click-to-detail and hover-to-source-preview.
  - US-007 screenshots cover route hover/focus and selected drawer state.
- Global thinking check
  - Docs now describe the reduced first screen, folded controls, route guide, previews, and desktop-only rule.
  - Visual regression checks the three highest-risk desktop states without adding mobile work.
  - No CCB, skill-hub, third-party body text, or local skill body content was copied into docs or code.
- why / what / how
  - Why: the finished refactor needs a repeatable desktop check and written project state.
  - What: docs and visual regression now describe and verify the clarity refactor.
  - How: Playwright CLI commands open the built app, save three screenshots, and fail on CLI errors or horizontal overflow.
- Learnings for future iterations:
  - Avoid Playwright CLI `run-code` for async snippets; command steps are easier to verify.
  - Keep visual regression focused on desktop states that map to actual user decisions.
---

## 2026-05-09T12:40:00+08:00 - US-001
- What was implemented
  - Converted `scripts/ralph/prd.json` from the completed clarity refactor to `KnowledgeGraphHorizontalMindMapRefactor`.
  - Kept `branchName` as `main` and created US-001 through US-008 for the horizontal mind-map refactor.
  - Verified active specs, CSS, and Playwright workflow keep mobile design, implementation, build, and testing out of scope.
  - Marked only US-001 as passing; US-002 through US-008 remain pending for later Ralph iterations.
- Files changed
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- Checks run
  - `Set-Location D:\agent-harness-lab; rg -n "mobile|移动端|@media \\(max-width|Mobile" apps/knowledge-graph DESIGN.md tasks scripts/ralph` passed; active hits are desktop-only scope statements, and old mobile expectations are under archive.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run typecheck` passed.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun test` passed, 16 tests.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run build` passed.
- Browser screenshot result
  - Not run for US-001 because this story only updates Ralph scope and validates existing desktop-only workflow; no UI surface changed.
- Hover / focus / active / click feedback
  - No component interaction changed in this story.
  - Existing interaction rules remain governed by `DESIGN.md` and the app-local `AGENTS.md`.
- Global thinking check
  - No page elements were added.
  - No layout region was pushed or squeezed.
  - The next user path is clearer because the active Ralph plan now points to the horizontal mind-map refactor before agent comparison work.
- why / what / how
  - Why: future iterations should not spend time on mobile work.
  - What: Ralph now tracks the desktop-only horizontal mind-map refactor.
  - How: `prd.json` contains the new story sequence and US-001 records the verified mobile boundary.
- Learnings for future iterations:
  - Before starting a new Ralph cycle, replace stale completed PRD JSON with the current highest-priority PRD and keep only the first unfinished story in scope.
---

## 2026-05-09T14:11:00+08:00 - US-002
- What was implemented
  - Locked the app into a desktop viewport shell with page-level overflow hidden.
  - Reworked the main page into a two-column desktop stage: intro on the left, graph workspace on the right, compact status strip below.
  - Moved supporting status content into the stage bottom row so primary content does not require page-level vertical scroll.
  - Tuned graph/detail column widths and status card text so the 1365x900 viewport is not squeezed.
- Files changed
  - `apps/knowledge-graph/src/styles/global.css`
  - `apps/knowledge-graph/src/styles/app-shell.css`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
  - `output/playwright/us002-desktop-stage.png`
- Checks run
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run typecheck` passed.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun test` passed, 16 tests.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run build` passed.
- Browser screenshot result
  - Local Playwright screenshot passed: `output/playwright/us002-desktop-stage.png`.
  - Playwright asserted `document.documentElement.scrollHeight === window.innerHeight` at 1365x900.
  - Playwright asserted the main stage bottom stays inside the viewport.
- Hover / focus / active / click feedback
  - No interaction behavior changed in this story.
  - Existing buttons still use border, underline, and background switch states from the current component rules.
- Global thinking check
  - Page-level vertical scroll is gone for the primary desktop stage.
  - Graph and detail now share one right-side workspace instead of appearing below the intro.
  - The bottom status strip is compact and no longer forces the user to scroll.
- why / what / how
  - Why: users should not scroll down to find the learning workspace.
  - What: the app is now a fixed desktop stage with intro, graph, detail, and status areas.
  - How: CSS sets a 100vh grid shell and constrains overflow to internal panels instead of the page.
- Learnings for future iterations:
  - For the horizontal refactor, keep page scroll locked and move dense content into internal panels or folded rails.
---

## 2026-05-09T14:17:00+08:00 - US-003
- What was implemented
  - Added horizontal scene state for `intro` and `map`.
  - Replaced anchor navigation with scene buttons so the top nav changes the visible scene.
  - Added visible `Intro / Map / Detail` markers.
  - Changed the continue action from an anchor to a button that slides left into the map workspace and focuses search.
- Files changed
  - `apps/knowledge-graph/src/components/layout/AppShell.tsx`
  - `apps/knowledge-graph/src/components/layout/GraphToolbar.tsx`
  - `apps/knowledge-graph/src/styles/app-shell.css`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
  - `output/playwright/us003-intro-scene.png`
  - `output/playwright/us003-map-scene.png`
- Checks run
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run typecheck` passed.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun test` passed, 16 tests.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run build` passed.
- Browser screenshot result
  - Local Playwright screenshots passed for intro and map scenes.
  - Playwright asserted the intro scene does not leak the map panel.
  - After clicking continue, Playwright asserted `data-scene="map"` and focus on `#graph-search-input`.
- Hover / focus / active / click feedback
  - Scene nav buttons use underline and active marker states.
  - Continue button uses the existing dark primary button hover/focus/active treatment.
  - Click feedback is visible through the scene marker switching from Intro to Map.
- Global thinking check
  - The first scene now shows only the intro, current path, first step, and continue action.
  - The map no longer competes with the intro before the user chooses to continue.
  - The first action is clear: click continue, then search or open the next step.
- why / what / how
  - Why: the page should feel like a focused lab, not a long scroll page.
  - What: there are now separate horizontal Intro and Map scenes.
  - How: React stores scene state, CSS slides the stage track, and focus is moved after click.
- Learnings for future iterations:
  - Put scene padding inside each scene so hidden scenes do not leak into the viewport.
---

## 2026-05-09T14:29:00+08:00 - US-004
- What was implemented
  - Added a central `Agent Harness` mind-map canvas rendered with React/CSS/SVG.
  - Added seven mechanism branches: Agent Loop, Tool System, Context System, Safety, Planning, Orchestration, MCP / Plugins.
  - Clicking a branch expands related feature nodes in the map.
  - Clicking a feature node opens the existing why / what / how detail panel.
  - Kept learning paths as secondary buttons above the mind map.
- Files changed
  - `apps/knowledge-graph/src/components/layout/AppShell.tsx`
  - `apps/knowledge-graph/src/components/graph/KnowledgeGraphCanvas.tsx`
  - `apps/knowledge-graph/src/styles/app-shell.css`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
  - `output/playwright/us004-mind-map-scene.png`
  - `output/playwright/us004-expanded-branch.png`
  - `output/playwright/us004-feature-detail.png`
- Checks run
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run typecheck` passed.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun test` passed, 16 tests.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run build` passed.
- Browser screenshot result
  - Local Playwright screenshots passed for mind-map scene, expanded Tool System branch, and feature detail.
  - Playwright asserted seven branches, four secondary learning path buttons, branch expansion, and detail why / how content.
- Hover / focus / active / click feedback
  - Branches and feature nodes invert background and underline on hover, focus, active, and selected state.
  - Clicking a branch changes the expanded feature strip.
  - Clicking a feature node selects it and opens detail.
- Global thinking check
  - The old linear route-guide no longer owns the first map viewport.
  - Learning paths remain visible as secondary buttons, not the main structure.
  - The first map action is clear: pick a mechanism branch, then pick a feature node.
- why / what / how
  - Why: learners need to see mechanism branches instead of a linear checklist.
  - What: the map now centers on Agent Harness and branches into mechanism clusters.
  - How: typed branch data maps to existing graph nodes; node click reuses the existing detail drawer.
- Learnings for future iterations:
  - Keep the mind map opaque and first in the map scene so older controls cannot visually compete with it.
---

## 2026-05-09T14:33:00+08:00 - US-005
- What was implemented
  - Reworked detail drawer layout into a horizontal panel strip.
  - Kept title and close action as the first detail panel so opening detail starts at the node identity.
  - Made why / what / how, progress, source entry, commands, references, quiz, and comparison reachable as horizontal cards.
  - Folded detail cards still expand on hover/focus and pin on click.
  - Source hover preview remains available inside the horizontal detail strip.
- Files changed
  - `apps/knowledge-graph/src/styles/app-shell.css`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
  - `output/playwright/us005-horizontal-detail.png`
  - `output/playwright/us005-source-preview-horizontal.png`
- Checks run
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run typecheck` passed.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun test` passed, 16 tests.
  - `Set-Location D:\agent-harness-lab\apps\knowledge-graph; bun run build` passed.
- Browser screenshot result
  - Local Playwright screenshots passed for horizontal detail and source preview.
  - Playwright asserted detail horizontal scroll, no page-level vertical overflow, and why / how content present.
- Hover / focus / active / click feedback
  - Detail group cards keep marker labels by default.
  - Hover/focus expands a group; click pins it with `data-open=true`.
  - Source preview opens on hover/focus in the references group.
- Global thinking check
  - Detail no longer pushes the page downward.
  - Long supporting content moves sideways inside the right panel.
  - The first visible detail action is still close, then why / what / how.
- why / what / how
  - Why: long details should not force vertical page scroll.
  - What: detail is now a horizontal panel strip.
  - How: CSS grid auto-columns and folded cards keep the content inside the existing detail region.
- Learnings for future iterations:
  - Do not focus a far-right header control in a horizontal strip; it can scroll the strip away from the first panel.
---
